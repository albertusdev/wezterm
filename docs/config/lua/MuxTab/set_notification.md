# `tab:set_notification(TEXT)`

{{since('nightly')}}

Sets the notification text for the tab.

This is a convenience wrapper around the reserved metadata key
`"wezterm.notification"`.

```lua
tab:set_notification '3'
```
