# `tab:clear_badge()`

{{since('nightly')}}

Clears the badge text and badge color from the tab.

This removes the reserved metadata keys `"wezterm.badge"` and
`"wezterm.badge_color"`.

```lua
tab:clear_badge()
```
