# `tab:clear_notification()`

{{since('nightly')}}

Clears the notification text and notification color from the tab.

This removes the reserved metadata keys `"wezterm.notification"` and
`"wezterm.notification_color"`.

```lua
tab:clear_notification()
```
