# `wezterm cli set-tab-notification TEXT`

{{since('nightly')}}

*Run `wezterm cli set-tab-notification --help` to see more help*

Sets a notification on a tab.

This is a convenience wrapper around the reserved metadata keys
`"wezterm.notification"` and `"wezterm.notification_color"`.
