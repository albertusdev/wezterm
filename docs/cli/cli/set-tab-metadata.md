# `wezterm cli set-tab-metadata`

{{since('nightly')}}

*Run `wezterm cli set-tab-metadata --help` to see more help*

Replaces the metadata map for a tab.

This is the low-level tab metadata interface. Convenience commands such as
`set-tab-badge` and `set-tab-notification` are thin wrappers around this API.
