# `wezterm cli set-tab-badge BADGE`

{{since('nightly')}}

*Run `wezterm cli set-tab-badge --help` to see more help*

Sets a badge on a tab.

This is a convenience wrapper around the reserved metadata keys
`"wezterm.badge"` and `"wezterm.badge_color"`.
